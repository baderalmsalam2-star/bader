# البيانات الحسّاسة — لا تُرفع إطلاقاً (أرقام مدنية، جوالات، أسماء، مفاتيح)
data/
*.db
*.db-wal
*.db-shm
secret.key

# اعتماديات وملفات مؤقتة
node_modules/
*.log

# ملف Cloudflare التنفيذي (يُنزَّل بشكل منفصل)
cloudflared.exe

# سكربتات فحص مؤقتة
_*.js
